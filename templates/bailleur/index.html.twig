{% extends 'base.html.twig' %}

{% block title %}Bailleur index{% endblock %}

{% block body %}
    <h1>Bailleur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bailleur in bailleurs %}
            <tr>
                <td>{{ bailleur.id }}</td>
                <td>{{ bailleur.nom }}</td>
                <td>{{ bailleur.description }}</td>
                <td>
                    <a href="{{ path('bailleur_show', {'id': bailleur.id}) }}">show</a>
                    <a href="{{ path('bailleur_edit', {'id': bailleur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('bailleur_new') }}">Create new</a>
{% endblock %}
