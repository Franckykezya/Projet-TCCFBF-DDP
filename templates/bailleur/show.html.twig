{% extends 'base.html.twig' %}

{% block title %}Bailleur{% endblock %}

{% block body %}
    <h1>{{ bailleur.nom }}</h1>

    <table class="table">
        <tbody>
        
            <tr>
                <th>Nom</th>
                <td>{{ bailleur.nom }}</td>
            </tr>
            <tr>
                <th>Siège</th>
                <td>{{ bailleur.siege }}</td>
            </tr>
               <tr>
                <th>Téléphone</th>
                <td>{{ bailleur.telephone }}</td>
            </tr>
               <tr>
                <th>Adresse mail</th>
                <td>{{ bailleur.mail }}</td>
            </tr>
               <tr>
                <th>Fax</th>
                <td>{{ bailleur.fax }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bailleur.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bailleur_index') }}">Retourner à la liste...</a></br>

    <a href="{{ path('bailleur_edit', {'id': bailleur.id}) }}">Modifier</a>

    {{ include('bailleur/_delete_form.html.twig') }}
{% endblock %}
