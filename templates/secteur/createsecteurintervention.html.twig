{% extends 'base.html.twig' %}

{% block title %}Ajout secteur{% endblock %}
    
{% block body %}
    
    <!-- BOOTSTRAP STYLES-->
    <link href="../assets/css/bootstrap.css" rel="stylesheet" />
    <!-- BOOTSTRAP SCRIPTS -->
    <script src="../assets/js/bootstrap.min.js"></script>

    <center><h1>Ajout bailleur</h1></center>

    {# {{ include('bailleur/_form.html.twig') }} #}

    <div class="col-md-12">

        <div class="col-md-3">
        
        </div>

        <div class="col-md-6">
            {{ form_start(formSecteurIntervention) }}
                {{ form_row(formSecteurIntervention.nom, {'label' : 'Nom du secteur', 'attr' : {'placeholder':"Nom du secteur"}}) }}
 
            <div class="form-row align-items-end pull-right">    

                    <button type="Submit" class="btn btn-info pull-right" name="" id="" value="">
                        <span class="glyphicon glyphicon-floppy-saved">_</span>Enregistrer
                    </button>

                {{ form_end(formSecteurIntervention) }}

                &nbsp; &nbsp; &nbsp;
                <a href="{{ path('secteur_liste') }}" class="btn btn-warning"><span class="glyphicon glyphicon-share-alt">_</span>Retour à la liste...</a></br>
            </div>

        </div>

        <div class="col-md-3">
        
        </div>
    </div> 

{% endblock %}


