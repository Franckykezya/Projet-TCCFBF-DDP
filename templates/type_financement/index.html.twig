{% extends 'base.html.twig' %}

{% block title %}Type financement{% endblock %}
    
{% block body %}

<style>
        .overlay {
        position: fixed;
        top: 0;
        bottom: 0;
        left: 0;
        right: 0;
        background: rgba(0, 0, 0, 0.7);
        transition: opacity 500ms;
        visibility: hidden;
        opacity: 0;
        }
        .overlay:target {
        visibility: visible;
        opacity: 1;
        }

        .popup {
        margin: 70px auto;
        padding: 20px;
        background: #fff;
        border-radius: 5px;
        width: 30%;
        position: relative;
        transition: all 5s ease-in-out;
        }

        .popup h2 {
        margin-top: 0;
        color: #333;
        font-family: Tahoma, Arial, sans-serif;
        }
        .popup .close {
        position: absolute;
        top: 20px;
        right: 30px;
        transition: all 200ms;
        font-size: 30px;
        font-weight: bold;
        text-decoration: none;
        color: red;
        }
        .popup .close:hover {
        color: #06D85F;
        }
        .popup .content {
        max-height: 30%;
        overflow: auto;
        }

        @media screen and (max-width: 700px){
        .box{
            width: 70%;
        }
        .popup{
            width: 70%;
        }
    }
</style>

    <div id="wrapper">
        <!-- /. NAV SIDE  -->
            <div id="page-wrapper" >
                <div id="page-inner">
<<<<<<< HEAD
                    <h1>Type financement</h1>

                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for type_financement in type_financements %}
                            <tr>
                                <td>{{ type_financement.nom }}</td>
                                <td>
                                    <a href="{{ path('type_financement_edit', {'id': type_financement.id}) }}" class="btn btn-info">Modifier</a>
                                    <td>
                                    <form method="post" action="{{ path('type_financement_delete', {'id': type_financement.id}) }}" onsubmit="return confirm('Vous voulez supprimer ce type de financement');">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_financement.id) }}">
                                        <button class="btn btn-danger">Supprimer</button>
                                    </form>
                                    </td>
                                </tr>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>

                <a href="#popup1" class="btn btn-success">Ajout type financement</a>
                <div id="popup1" class="overlay">
                    <div class="popup">
                        <h2>Ajout type financement</h2>
                        <a class="close" href="{{ path('type_financement_index') }}">&times;</a>
                        <!-- input -->
                        {{ include('type_financement/_form.html.twig') }}
                        <a href="{{ path('type_financement_index') }}">Liste</a>     
                    </div>
                </div>   
            </div>
        </div>
=======
            <h1>TypeFinancement index</h1>

            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for type_financement in type_financements %}
                    <tr>
                        <td>{{ type_financement.id }}</td>
                        <td>{{ type_financement.nom }}</td>
                        <td>
                            <a href="{{ path('type_financement_show', {'id': type_financement.id}) }}">show</a>
                            <a href="{{ path('type_financement_edit', {'id': type_financement.id}) }}">edit</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

    <a href="{{ path('type_financement_new') }}">Create new</a>
    </div>
    </div>
>>>>>>> 6b65a20713a8a715e882d247422dcfad39a2750a
    </div>

{% endblock %}
            