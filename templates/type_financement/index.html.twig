{% extends 'base.html.twig' %}

{% block title %}Type financement{% endblock %}

{% block body %}
    <center> <h1> Liste des types finacements</h1></center>

<div class="col-md-12">

        <div class="col-md-3">
        
        </div>

        <div class="col-md-6">

        <table class="table table-striped">
            <thead>
                <tr class="table-info">       
                    <th scope="col">Nom</th>  
                    <th scope="col">Actions</th> 
                </tr>
            </thead>
            <tbody>
                {% for type_financement in type_financements %}
                <tr>
                    <td>{{ type_financement.nom }}</td>
                        <td>
                        <div class="form-row align-items-end">
                            <a href="{{ path('type_financement_edit', {'id': type_financement.id}) }}" class="btn btn-info"><span class="glyphicon glyphicon-edit">_</span>Modifier</a>                
                            &nbsp; &nbsp; &nbsp;
                            <form method="post" action="{{ path('secteur_delete', {'id': type_financement.id}) }}" onsubmit="return confirm('Vous voulez supprimer ce type financement?');">
                                <input type="hidden" name="_method" value="DELETE">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_financement.id) }}">
                                <button class="btn btn-danger"><span class="glyphicon glyphicon-trash">_</span>Supprimer</button>
                            </form>     
                        </div>
                        </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">Aucun type financement...</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
     </div>

    <div class="col-md-3">
        
    </div>
</div>

    <center>
        <a href="{{ path('type_financement_new') }}" class="btn btn-success"><span class="glyphicon glyphicon-plus">_</span>Ajout type financement</a>
    </center>

{% endblock %}

